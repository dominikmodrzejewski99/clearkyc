services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: clearkyc
      POSTGRES_USER: clearkyc
      POSTGRES_PASSWORD: clearkyc
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
